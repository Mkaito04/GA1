# 3.7 AMR搬送スケジューリングの詳細（現状まとめ）

## 3.7.1 各AMRの情報保持

各AMRは以下の情報を保持する：

- **現在位置のノード** (`current_node`): AMRが現在いるノード
- **次に利用可能になる時刻** (`available_time`): AMRが次の搬送を開始できる時刻
- **最大積載容量** (`max_capacity`): 同時に運搬可能なジョブの最大数（本システムでは3）
- **搬送・回送のタイムライン** (`timeline`): 各搬送・回送の詳細情報
  - 開始時刻 (`start_time`)
  - 終了時刻 (`end_time`)
  - 搬送元ノード (`from_node`)
  - 搬送先ノード (`to_node`)
  - 運搬ジョブ (`job_names`: リスト形式)
  - アクションタイプ (`action_type`: 'transport' または 'return')

これらの情報は`AMRState`クラスで管理され、AMRの状態を追跡する。

## 3.7.2 経路コストの取得

ノード間の移動時間（経路コスト）は、経路グラフから取得される。`get_route_cost`メソッドにより、2つのノード間の最短経路のコスト（時間）を取得する。経路が存在しない場合は0を返す。

経路グラフは、各ノードペアについて最短経路のコストを保持し、中間ノードを含む経路シーケンスも`get_route_sequence`メソッドにより取得可能である。これにより、AMRが経路上の中間ノードでジョブを拾えるようになっている。

## 3.7.3 バッチ搬送の実装

本研究では、AMRの搬送効率を向上させるため、複数のジョブをまとめて運ぶバッチ搬送を採用している。

### バッチ形成アルゴリズム

バッチ形成は、以下の貪欲的アルゴリズムにより行われる：

1. **距離ベースの優先順位付け**: AMRの現在位置から遠い搬送元ノードを優先的に選択
2. **前工程完了時刻の考慮**: 距離が同じ場合は、前工程完了時刻が早いジョブを優先
3. **時間的制約の考慮**: 前工程完了時刻が現在時刻から10.0以内のジョブを同一バッチに含める

各バッチは、AMRの容量制約を満たす必要があり、本システムでは最大3個のジョブまで同時に搬送可能である。

### 動的バッチング

AMRが経路計画を実行した後、移動中に前工程が完了するジョブを検索し、経路上で拾える場合は動的にバッチに追加する。これにより、以下の利点がある：

1. **搬送回数の削減**: 複数のジョブを一度に運ぶことで、AMRの移動回数を削減
2. **AMRの稼働率向上**: 空荷移動時間を削減し、AMRの稼働率を向上
3. **全体のmakespan短縮**: 搬送効率の向上により、全体の完了時間を短縮

### 経路計画

各バッチでは、複数のジョブを同時に運搬するため、**最近傍法（Nearest Neighbor）**を使用して最適な訪問順序を決定する。経路計画では、以下の点を考慮する：

1. **搬送元ノードの訪問順序**: AMRの現在位置から最も近い搬送元ノードを順次訪問
2. **搬送先ノードの訪問順序**: 搬送元ノードの訪問後、最も近い搬送先ノードを順次訪問
3. **中間ノードの展開**: 経路上の中間ノードも訪問順序に含め、経路上でジョブを拾えるようにする

各バッチでは、複数のジョブを同時に運搬するため、最も遠い搬送先への移動時間を搬送時間とする。これにより、バッチ内の全ジョブが同時に到着する。

### バッチ処理の適用範囲

バッチ処理は以下の工程間搬送に適用される：

- **START → 工程A**: AMR1が最大3つまで同時に運搬
- **工程A → 工程B**: AMR2が最大3つまで同時に運搬
- **工程B → 工程C**: AMR3が最大3つまで同時に運搬
- **工程C → 工程D**: AMR3が最大3つまで同時に運搬
- **工程D → END**: AMR3が最大3つまで同時に運搬（真のバッチ処理）

## 3.7.4 回送時間の考慮

AMRが搬送元に到着するために必要な空荷移動時間（回送時間）を考慮する。回送開始時刻は、AMRの利用可能時刻と前工程完了時刻から回送時間を引いた時刻の最大値として決定される。これにより、AMRが前工程完了と同時に搬送元に到着できるようにスケジューリングされる。

具体的には、以下の式で回送開始時刻を計算する：

```
return_start = max(amr_ready_time, max_prev_end_time - return_time)
```

ただし、`return_start < amr_ready_time`の場合は、`amr_ready_time`を使用する。

## 3.8 アルゴリズムの実行フロー

本手法の実行フローは以下の通りである：

1. **初期化**: 個体数N=100、最大世代数300で初期群を生成
   - 各個体は、ジョブ順序（`job_order`）とマシン割り当て（`machine_assignment`）から構成される
   - 経路計画は常に最近傍法を使用するため、個体には含まれない

2. **評価**: 全個体の適応度（makespan）を計算
   - makespanは、最後のジョブのENDノード到着時刻として定義される
   - 搬送時間を考慮したスケジュール評価を実行

3. **最良解の更新**: 現在の最良解より良い個体があれば更新

4. **次世代の生成**:
   - **エリート保存**: 最良個体を次世代に保存
   - **残りN-1個体を生成**:
     1. トーナメント選択（トーナメントサイズ=3）で親を2つ選択
     2. 交叉確率0.8で交叉を実行
        - ジョブ順序: 順序交叉（OX）
        - マシン割り当て: 一様交叉
     3. 突然変異確率0.1で突然変異を実行
        - ジョブ順序: 2つのジョブをランダムにスワップ
        - マシン割り当て: 各工程のマシン割り当てをランダムに変更（全体の10%）

5. **終了判定**: 最大世代数に達するまで2-4を繰り返す

## 3.9 出力と可視化

最適化結果は以下の形式で出力される：

1. **テキスト出力**: 各マシン・AMRのスケジュールを時系列で記録したテキストファイル（`execution_log_transport.txt`）
   - マシンスケジュール: 各マシンで処理されるジョブとその開始・終了時刻
   - AMRスケジュール: 各AMRの搬送・回送のタイムライン

2. **ガントチャート**: マシンとAMRのスケジュールを時系列で可視化した画像ファイル（`ganttchart_collision.jpeg`）
   - 上部: マシンスケジュール（各ジョブの処理時間を色分けして表示）
   - 下部: AMRスケジュール（搬送と回送を区別して表示）

3. **進化履歴**: 世代ごとの最良適応度をテキストで出力
   - 最初の10世代と最後の10世代を表示
   - 進化グラフの画像出力機能は現在実装されていないが、`fitness_history`にデータが記録されている

これにより、最適化過程と結果を定量的・視覚的に評価できる。

## 3.10 本手法の特徴

本手法の主な特徴は以下の通りである：

1. **同時最適化**: ジョブ順序とマシン割り当てを同時に最適化することで、解の質が向上する

2. **搬送時間の考慮**: AMRによる搬送時間を考慮することで、実用的なスケジュールを生成できる

3. **容量制約の考慮**: AMRの容量制約（最大3個）を考慮することで、現実的な搬送スケジュールを生成できる

4. **回送時間の考慮**: 空荷移動時間を考慮することで、より正確なmakespanを計算できる

5. **動的バッチング**: AMRが移動中に完了するジョブを動的にバッチに追加することで、搬送効率を向上

6. **最近傍法による経路計画**: バッチ内の複数ジョブの搬送元・搬送先ノードの訪問順序を最近傍法で決定することで、移動時間を最小化

7. **中間ノードの考慮**: 経路上の中間ノードも訪問順序に含めることで、経路上でジョブを拾えるようにする

8. **可視化機能**: ガントチャートにより、結果を直感的に理解できる

## 3.11 実装上の重要な変更点

### GAの最適化対象の簡素化

初期実装では、GAがジョブ順序、マシン割り当て、および経路戦略（'direct', 'nearest', 'optimized'）を同時に最適化していた。しかし、以下の理由により、経路戦略の最適化は削除された：

1. **'optimized'と'nearest'の同一性**: 両方とも最近傍法を使用しており、実質的に同じ動作をしていた
2. **'direct'の非最適化性**: 直接経路は最適化ではなく、単純な経路選択であった
3. **探索空間の削減**: 経路戦略を削除することで、GAの探索空間を削減し、最適化を効率化

現在の実装では、経路計画は常に最近傍法を使用し、GAはジョブ順序とマシン割り当てのみを最適化する。これにより、GAの探索空間が削減され、最適化が効率化されている。

### 真のバッチ処理の実装

D→ENDの搬送処理では、初期実装ではバッチ内の各ジョブを個別に処理していた（疑似バッチ処理）。現在の実装では、`_transport_batch_between_processes`関数を使用して、複数のジョブをまとめて運ぶ真のバッチ処理を実装している。これにより、以下の利点がある：

1. **1つの経路計画**: バッチ内の全ジョブに対して1つの経路計画を実行
2. **1つの回送**: AMRが1回だけ回送を行う
3. **1つの搬送**: バッチ内の全ジョブを同時に運搬

### 動的バッチングの拡張

初期実装では、経路計画に含まれるノードのみをチェックしていたため、経路上で拾えるジョブを見逃す可能性があった。現在の実装では、以下の拡張を行っている：

1. **すべてのリソースの確認**: すべての前工程マシンの出口ポートノードを確認
2. **中間ノードの考慮**: 経路計画の中間ノードも訪問時刻の計算に含める
3. **最短到着時刻の計算**: 経路計画の各ノードから対象ノードへの最短到着時刻を計算

これにより、AMRが移動中に完了するジョブをより効果的に拾えるようになっている。

